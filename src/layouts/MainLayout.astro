---
import { SITE_DEFAULT_CONFIG } from "@/global/site-info";

import Header from "@/common/layout/header/Header.astro";
import Footer from "@/common/layout/footer/Footer.astro";

import GTMBody from "@/analytics/Google/GTMBody.astro";
import BottomBar from "@/common/navigation/BottomBar";

import BaseHead from "@/seo/BaseHead.astro";

const { lang } = SITE_DEFAULT_CONFIG;
---

<!doctype html>
<html lang={lang}>
	<BaseHead>
		<slot name="head" />
	</BaseHead>
	<body>
		<GTMBody />
		<Header />
		<main>
			<slot />
			<BottomBar client:only="react" />
		</main>
		<Footer />
	</body>
</html>
