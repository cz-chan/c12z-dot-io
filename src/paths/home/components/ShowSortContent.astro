---
import styles from "./show-content.module.css"
import { updatedSectionLists } from "../lib/sort-content-in-home";
import SectionLists from "./SectionLists.astro";

---

<section class={styles.list}>

  {
    updatedSectionLists.map(({ style, description, href, items, label, totalCount }) => (
      <SectionLists
        href={href}
        style={style}
        label={label}
        description={description}
        items={items}
        totalCount={totalCount}
      />
    ))
  }
</section>
