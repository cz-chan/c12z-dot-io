---
// import { getCollection } from "astro:content";

import { PAGES } from "@/global/pages-info.ts";
import MainLayout from "@/layouts/MainLayout.astro";
import GoBackInTop from "@/ui/go-back-in-top/GoBackInTop.astro";

import PagesSEO from "@/seo/PagesSEO.astro";

// const essays = await getCollection("essay");

const ESSAY_PAGE = PAGES.essay;
---

<MainLayout>
	<PagesSEO
		slot="head"
		pageTitle={ESSAY_PAGE.title}
		pageDescription={ESSAY_PAGE.description}
		ogImage={ESSAY_PAGE.ogImage}
		ogImageAlt={ESSAY_PAGE.ogImageAlt}
	/>
	<GoBackInTop
		headPage="/"
		title="ir a la home"
	/>
	<section>
		<h1>Ensayos</h1>
		<p>Por ahora, no tengo ningún ensayo que mustrarte! 🙂</p>
	</section>

	<section>
		{
			/*essays.map((essay) => {
        const { data, id } = essay;
        const { title, description } = data;

        return (
          <article class="">
            <div>
              <h1>{title}</h1>
              <p >{description}</p>
            </div>
            <a
              

              href={`/ensayos/${id}`}
            >
              Ir al post
            </a>
          </article>
        );
      })
    */
		}
	</section>
</MainLayout>
